#export YOUTUBE_API_KEY=your_key_here   # only needed once per terminal session
npm run loadNewVideos                   # pulls new videos from YouTube
npm run extractGames                    # tries to match chess games from titles
npm run generate                        # rebuilds generated/ from db/
npm run dev                             # starts local preview server
